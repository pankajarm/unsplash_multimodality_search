STEPS FOR BACKEND
    INSTALL!!!!!!
    sudo apt-get update
    sudo apt-get upgrade
    sudo apt install python3-pip
    which python3

    pip3 install pipenv
    # add path to ~/bashrc
    export PATH="$PATH:$HOME/.local/bin"

    #execute bashrc
    . ~/.bashrc
    which virtualenv

    git clone https://github.com/pankajarm/unsplash_multimodality_search.git
    cd unsplash_multimodality_search/backend/
    pipenv shell
    pipenv install
    "DON'T WORRY, GIVE 5 MINUTE AS IT IS SLIENT INSTALL!"

    scp -i "YOUR_KEY_PAIR.pem" unsplash-finetuned-onnx.zip ubuntu@YOUR_MACHINE_IP.MACHINE_REGION.compute.amazonaws.com:~/unsplash_multimodality_search/backend


    sudo apt install unzip
    unzip unsplash-finetuned-onnx.zip
    clt+d

    RUN!!!!!
    screen
    pipenv shell
    python3 -m clip_server finetuned_clip.yml
    clt+a+d


STEPS for FRONTEND
    INSTALLATION!
    (Steps START only if it's a different machine)
    sudo apt-get update
    sudo apt-get upgrade
    sudo apt install python3-pip
    which python3

    pip3 install pipenv
    # add path to ~/bashrc
    export PATH="$PATH:$HOME/.local/bin"
    #execute bashrc
    . ~/.bashrc
    which virtualenv
    sudo apt install unzip

    git clone https://github.com/pankajarm/unsplash_multimodality_search.git
    cd unsplash_multimodality_search/frontend/
    (Steps ENDS only if it's a different machine)

    LOCAL (on your local terminal)
    scp -i "YOUR_KEY_PAIR.pem" resize_images.zip ubuntu@YOUR_MACHINE_IP.MACHINE_REGION.compute.amazonaws.com:~/unsplash_multimodality_search/frontend
    scp -i "YOUR_KEY_PAIR.pem" data_da.bin.zip ubuntu@eYOUR_MACHINE_IP.MACHINE_REGION.compute.amazonaws.com:~/unsplash_multimodality_search/frontend


    unzip resize_images.zip
    unzip data_da.bin.zip

    rm resize_images.zip
    rm data_da.bin.zip
    
    pipenv shell
    pipenv install
    streamlit hello
    clt+z/x
    clt+d


    RUN!!!!
    screen
    pipenv shell
    streamlit run ui.py
    clt+a+d
